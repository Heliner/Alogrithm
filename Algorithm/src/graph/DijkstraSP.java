package graph;

public class DijkstraSP {

	// v为起始点
	int[] dijkstra(int matrix[][] , int v) {
		//1.从起点到其他没有确定最短距离的点的距离如果较小就更新距离
		//2.在本次中找出距离最短的路径，将该点从未确定距离的点集中移除，判断最小的distTo[i]，
		//1.更新所有和i有关的路径
		int distTo[] = new int[10];
		return distTo;
	}
}
